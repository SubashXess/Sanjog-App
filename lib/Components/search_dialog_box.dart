import 'package:flutter/material.dart';

class SearchDialogBox extends StatelessWidget {
  const SearchDialogBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        // child: ,
        );
  }
}
